// Translations
const translations = {
    ka: {
      heroTitle: "შენი ავტომობილი — ჩვენი პასუხისმგებლობა",
      heroSub: "შეთანხმებულ დროში, უსაფრთხოდ გზაზე",
      trustLine: "ჩვენ ვიღებთ პასუხისმგებლობას, რომ თქვენი ავტომობილი შეთანხმებულ დროში ჩაგბარდებათ და გზაზე უსაფრთხოდ იმოძრავებს.",
      paymentInfo: "შეფასების საფასური: 10-20₾\nსაბოლოო თანხას იხდით სამუშაოს დასრულების შემდეგ",
      packages: {
        standard: { title: "სტანდარტული", short: "სწრაფი გადაწყვეტა მარტივი პრობლემებისთვის", desc: "✔️ დიაგნოსტიკა\n✔️ ძირითადი ტექნიკური შეკეთება\n✔️ საჭირო დეტალების შერჩევა", process: "1. შეფასება\n2. სწრაფი შეკეთება\n3. ჩაბარება", time: "მოკლე ვადებში", price: "ხელმისაწვდომი", cta: "დაიწყე WhatsApp-ში" },
        premium: { title: "პრემიუმი", short: "სრული კონტროლი და ხარისხიანი შედეგი", desc: "✔️ სრული დიაგნოსტიკა\n✔️ ყველა პრობლემის აღმოჩენა\n✔️ დეტალური შეკეთება", process: "1. სრული შემოწმება\n2. პრობლემების იდენტიფიკაცია\n3. სრულყოფილი შეკეთება", time: "ოპტიმალური", price: "საშუალო", cta: "შეკვეთა WhatsApp-ში" },
        express: { title: "ექსპრესი", short: "მაქსიმალური კომფორტი — ჩვენ ყველაფერს ვაგვარებთ", desc: "✔️ სრული პასუხისმგებლობა ავტომობილზე\n✔️ ყველა ტექნიკური პრობლემის გადაწყვეტა\n✔️ მინიმალური ჩართულობა შენგან", process: "1. სრული შეფასება\n2. სრული მართვა ჩვენი მხრიდან\n3. მზად ავტომობილის ჩაბარება", time: "რაც შეიძლება სწრაფად", price: "პრემიუმ", cta: "დაწყება ახლავე", recommended: "რეკომენდირებული" }
      },
      stats: ["25+ წლიანი გამოცდილება", "1000+ შეკეთებული ავტომობილი", "სწრაფი მომსახურება"],
      gallery: "გალერეა",
      aboutTitle: "ჩვენს შესახებ",
      aboutText: "ჩვენი ძალა გამოცდილებაშია — ნებისმიერ პრობლემას ვაგვარებთ უმოკლეს დროში",
      aboutEquip: "✔️ სამღებრო კამერა | ✔️ დიაგნოსტიკა | ✔️ თანამედროვე ტექნიკა",
      contactTitle: "კონტაქტი",
      phoneLabel: "ტელეფონი",
      addressLabel: "მისამართი",
      hoursLabel: "სამუშაო საათები",
      social: { insta: "https://www.instagram.com/rezovich_auto/", fb: "https://www.facebook.com/profile.php?id=61583395648052", tiktok: "https://www.tiktok.com/@imermotorsi", linkedin: "https://www.linkedin.com/in/imermotors/" }
    },
    en: {
      heroTitle: "Your car — our responsibility",
      heroSub: "On time, safely on the road",
      trustLine: "We take responsibility to deliver your car on time and ensure it drives safely.",
      paymentInfo: "Assessment fee: 10-20 GEL\nFinal payment after completion",
      packages: {
        standard: { title: "Standard", short: "Quick solution for simple problems", desc: "✔️ Diagnostics\n✔️ Basic technical repair\n✔️ Parts selection", process: "1. Assessment\n2. Fast repair\n3. Delivery", time: "Short terms", price: "Affordable", cta: "Start on WhatsApp" },
        premium: { title: "Premium", short: "Full control and quality result", desc: "✔️ Full diagnostics\n✔️ Detect all issues\n✔️ Detailed repair", process: "1. Complete inspection\n2. Problem identification\n3. Perfect repair", time: "Optimal", price: "Medium", cta: "Order on WhatsApp" },
        express: { title: "Express", short: "Maximum comfort — we handle everything", desc: "✔️ Full responsibility\n✔️ Solve all technical issues\n✔️ Minimal involvement", process: "1. Full assessment\n2. Complete management by us\n3. Ready car delivery", time: "As fast as possible", price: "Premium", cta: "Start now", recommended: "Recommended" }
      },
      stats: ["25+ years of experience", "1000+ cars repaired", "Fast service"],
      gallery: "Gallery",
      aboutTitle: "About Us",
      aboutText: "Our strength is experience — we solve any problem in the shortest time",
      aboutEquip: "✔️ Painting booth | ✔️ Diagnostics | ✔️ Modern tools",
      contactTitle: "Contact",
      phoneLabel: "Phone",
      addressLabel: "Address",
      hoursLabel: "Working hours",
      social: { insta: "https://www.instagram.com/rezovich_auto/", fb: "https://www.facebook.com/profile.php?id=61583395648052", tiktok: "https://www.tiktok.com/@imermotorsi", linkedin: "https://www.linkedin.com/in/imermotors/" }
    },
    ru: {
      heroTitle: "Ваш автомобиль — наша ответственность",
      heroSub: "В срок, безопасно на дороге",
      trustLine: "Мы берем ответственность за своевременную и безопасную работу вашего авто.",
      paymentInfo: "Плата за оценку: 10-20 GEL\nОкончательная оплата после завершения",
      packages: {
        standard: { title: "Стандарт", short: "Быстрое решение простых проблем", desc: "✔️ Диагностика\n✔️ Базовый ремонт\n✔️ Подбор деталей", process: "1. Оценка\n2. Быстрый ремонт\n3. Выдача", time: "Короткие сроки", price: "Доступно", cta: "Начать в WhatsApp" },
        premium: { title: "Премиум", short: "Полный контроль и качество", desc: "✔️ Полная диагностика\n✔️ Выявление всех проблем\n✔️ Детальный ремонт", process: "1. Полный осмотр\n2. Идентификация проблем\n3. Идеальный ремонт", time: "Оптимально", price: "Средне", cta: "Заказать в WhatsApp" },
        express: { title: "Экспресс", short: "Максимальный комфорт — мы всё решаем", desc: "✔️ Полная ответственность\n✔️ Решение всех проблем\n✔️ Минимум участия", process: "1. Полная оценка\n2. Полное управление нами\n3. Выдача готового авто", time: "Максимально быстро", price: "Премиум", cta: "Начать сейчас", recommended: "Рекомендуемый" }
      },
      stats: ["25+ лет опыта", "1000+ отремонтированных авто", "Быстрое обслуживание"],
      gallery: "Галерея",
      aboutTitle: "О нас",
      aboutText: "Наша сила в опыте — решаем любую проблему в кратчайшие сроки",
      aboutEquip: "✔️ Покрасочная камера | ✔️ Диагностика | ✔️ Современное оборудование",
      contactTitle: "Контакты",
      phoneLabel: "Телефон",
      addressLabel: "Адрес",
      hoursLabel: "Часы работы",
      social: { insta: "https://www.instagram.com/rezovich_auto/", fb: "https://www.facebook.com/profile.php?id=61583395648052", tiktok: "https://www.tiktok.com/@imermotorsi", linkedin: "https://www.linkedin.com/in/imermotors/" }
    }
  };
  
  // Gallery images from local folder (mercedes.jpg, bmw.jpg, audi.jpg repeated)
  const galleryImages = [
    './assets/g3.jpeg',
    './assets/g4.jpeg',
    './assets/g6.jpeg',
    './assets/g1.jpeg',
    './assets/g0.jpeg',
    './assets/g2.jpeg',
    './assets/g5.jpeg'
  ];
  
  let currentLocale = 'ka';
  let activeCardId = null;
  let currentSlide = 0;
  const totalSlides = galleryImages.length;
  
  // DOM Elements
  const heroTitle = document.getElementById('heroTitle');
  const heroSub = document.getElementById('heroSub');
  const trustLineText = document.getElementById('trustLineText');
  const paymentInfo = document.getElementById('paymentInfo');
  const galleryTitle = document.getElementById('galleryTitle');
  const aboutTitle = document.getElementById('aboutTitle');
  const aboutText = document.getElementById('aboutText');
  const aboutEquip = document.getElementById('aboutEquip');
  const contactTitle = document.getElementById('contactTitle');
  const phoneLabel = document.getElementById('phoneLabel');
  const addressLabel = document.getElementById('addressLabel');
  const hoursLabel = document.getElementById('hoursLabel');
  const socialIcons = document.getElementById('socialIcons');
  const cardsContainer = document.getElementById('cardsContainer');
  const statsGrid = document.getElementById('statsGrid');
  const gallerySlides = document.getElementById('gallerySlides');
  const imageCounter = document.getElementById('imageCounter');
  const langBtn = document.getElementById('langBtn');
  const langDropdown = document.getElementById('langDropdown');
  const mobileMenu = document.getElementById('mobileMenu');
  const burgerBtn = document.getElementById('burgerBtn');
  const closeMenu = document.getElementById('closeMenu');
  const floatWhatsApp = document.getElementById('floatWhatsApp');
  const prevSlideBtn = document.getElementById('prevSlide');
  const nextSlideBtn = document.getElementById('nextSlide');
  
  // Clock
  function updateClock() {
    const now = new Date();
    const tbilisi = new Date(now.toLocaleString("en-US", { timeZone: "Asia/Tbilisi" }));
    document.getElementById('time').innerText = tbilisi.toLocaleTimeString('en-GB');
    document.getElementById('date').innerText = tbilisi.toLocaleDateString('en-GB');
  }
  setInterval(updateClock, 1000);
  updateClock();
  
  // Build package cards
  function buildCards() {
    const t = translations[currentLocale].packages;
    cardsContainer.innerHTML = '';
    const packages = ['standard', 'premium', 'express'];
    packages.forEach((key) => {
      const pkg = t[key];
      const isRec = key === 'express' && pkg.recommended;
      const cardDiv = document.createElement('div');
      cardDiv.className = `card ${isRec ? 'recommended' : ''}`;
      cardDiv.setAttribute('data-package', key);
      cardDiv.innerHTML = `
        ${isRec ? `<div class="recommended-badge">${pkg.recommended}</div>` : ''}
        <div class="card-icon">${key === 'standard' ? '🔰' : key === 'premium' ? '⚙️' : '🔥'}</div>
        <div class="card-title">${pkg.title}</div>
        <div class="card-short">${pkg.short}</div>
        <div class="expand-icon ${activeCardId === key ? 'open' : ''}"><i class="fas fa-chevron-down"></i></div>
        <div class="card-expanded ${activeCardId === key ? 'open' : ''}">
          <div class="expanded-content">
            <h4>${currentLocale === 'ka' ? 'აღწერა' : currentLocale === 'en' ? 'Description' : 'Описание'}</h4>
            <p style="white-space:pre-line">${pkg.desc}</p>
            <h4>${currentLocale === 'ka' ? 'პროცესი' : currentLocale === 'en' ? 'Process' : 'Процесс'}</h4>
            <p style="white-space:pre-line">${pkg.process}</p>
            <div class="price-time"><span>⏱ ${pkg.time}</span><span>💰 ${pkg.price}</span></div>
            <button class="whatsapp-btn" data-pkg="${key}" data-title="${pkg.title}"><i class="fab fa-whatsapp"></i> ${pkg.cta}</button>
          </div>
        </div>
      `;
      cardDiv.addEventListener('click', (e) => {
        if (e.target.classList.contains('whatsapp-btn')) return;
        if (activeCardId === key) {
          activeCardId = null;
        } else {
          activeCardId = key;
          if (typeof gtag !== 'undefined') gtag('event', 'card_click', { package: key });
        }
        buildCards();
      });
      const btn = cardDiv.querySelector('.whatsapp-btn');
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const pkgTitle = btn.getAttribute('data-title');
        const msg = `${currentLocale === 'ka' ? 'გამარჯობა' : currentLocale === 'en' ? 'Hello' : 'Здравствуйте'},\n${currentLocale === 'ka' ? 'მინდა შეფასება.' : currentLocale === 'en' ? 'I would like an assessment.' : 'Хочу получить оценку.'}\n\n${currentLocale === 'ka' ? 'არჩეული პაკეტი' : currentLocale === 'en' ? 'Selected package' : 'Выбранный пакет'}: ${pkgTitle}\n\n${currentLocale === 'ka' ? 'გთხოვთ მომწეროთ დეტალები.' : currentLocale === 'en' ? 'Please send me details.' : 'Пожалуйста, напишите детали.'}`;
        window.open(`https://wa.me/995593082255?text=${encodeURIComponent(msg)}`, '_blank');
        if (typeof gtag !== 'undefined') gtag('event', 'whatsapp_click', { package: key });
      });
      cardsContainer.appendChild(cardDiv);
    });
  }
  
  // Build stats with counter animation
  function buildStats() {
    const stats = translations[currentLocale].stats;
    statsGrid.innerHTML = stats.map((text, idx) => `<div class="stat-item"><h2 class="counter" data-target="${idx===0?25:idx===1?1000:24}">0</h2><p>${text}</p></div>`).join('');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const counters = document.querySelectorAll('.counter');
          counters.forEach(c => {
            const target = parseInt(c.dataset.target);
            let current = 0;
            const step = Math.ceil(target / 50);
            const interval = setInterval(() => {
              current += step;
              if (current >= target) {
                c.innerText = target + (c.dataset.target == '1000' ? '+' : '');
                clearInterval(interval);
              } else c.innerText = current;
            }, 25);
          });
          observer.disconnect();
        }
      });
    }, { threshold: 0.3 });
    observer.observe(statsGrid);
  }
  
  // Gallery rendering
  function renderGallery() {
    gallerySlides.innerHTML = galleryImages.map((img, i) => `<div class="gallery-slide" data-index="${i}"><img src="${img}" loading="lazy" alt="gallery ${i}"></div>`).join('');
    document.querySelectorAll('.gallery-slide').forEach(slide => {
      slide.addEventListener('click', () => {
        const imgSrc = slide.querySelector('img').src;
        const lb = document.createElement('div');
        lb.className = 'lightbox';
        lb.innerHTML = `<img src="${imgSrc}">`;
        lb.addEventListener('click', () => lb.remove());
        document.body.appendChild(lb);
      });
    });
    updateGallery();
  }
  
  function updateGallery() {
    const slides = document.querySelectorAll('.gallery-slide');
    if (slides.length) {
      gallerySlides.style.transform = `translateX(-${currentSlide * 100}%)`;
      imageCounter.innerText = `${currentSlide + 1} / ${totalSlides}`;
    }
  }
  
  function nextSlide() {
    if (currentSlide < totalSlides - 1) { currentSlide++; updateGallery(); }
  }
  function prevSlide() {
    if (currentSlide > 0) { currentSlide--; updateGallery(); }
  }
  
  // Apply locale to all static texts
  function applyLocale() {
    const t = translations[currentLocale];
    heroTitle.innerText = t.heroTitle;
    heroSub.innerText = t.heroSub;
    trustLineText.innerText = t.trustLine;
    paymentInfo.innerHTML = t.paymentInfo.replace(/\n/g, '<br>');
    galleryTitle.innerText = t.gallery;
    aboutTitle.innerText = t.aboutTitle;
    aboutText.innerText = t.aboutText;
    aboutEquip.innerHTML = t.aboutEquip;
    contactTitle.innerText = t.contactTitle;
    phoneLabel.innerText = t.phoneLabel;
    addressLabel.innerText = t.addressLabel;
    hoursLabel.innerText = t.hoursLabel;
    socialIcons.innerHTML = `<a href="${t.social.insta}" target="_blank"><i class="fab fa-instagram"></i></a>
                             <a href="${t.social.fb}" target="_blank"><i class="fab fa-facebook"></i></a>
                             <a href="${t.social.tiktok}" target="_blank"><i class="fab fa-tiktok"></i></a>
                             <a href="${t.social.linkedin}" target="_blank"><i class="fab fa-linkedin"></i></a>`;
    // Update mobile and nav link texts
    document.querySelectorAll('.mobile-nav-link, .nav-links a[data-nav]').forEach(link => {
      const section = link.getAttribute('data-nav');
      if (section === 'about') link.innerText = currentLocale === 'ka' ? 'ჩვენს შესახებ' : (currentLocale === 'en' ? 'About' : 'О нас');
      if (section === 'gallery') link.innerText = t.gallery;
      if (section === 'contact') link.innerText = t.contactTitle;
    });
    langBtn.innerHTML = `🌐 ${currentLocale.toUpperCase()}`;
    buildCards();
    buildStats();
  }
  
  function changeLocale(locale) {
    currentLocale = locale;
    applyLocale();
    const newPath = `/${locale}`;
    window.history.pushState({}, '', newPath);
    document.documentElement.lang = locale;
    document.title = `ImerMotors - ${locale === 'ka' ? 'ავტოსერვისი' : locale === 'en' ? 'Car Service' : 'Автосервис'}`;
  }
  
  // Event listeners
  document.querySelectorAll('[data-nav]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      const section = btn.getAttribute('data-nav');
      const el = document.getElementById(`${section}Section`);
      if (el) el.scrollIntoView({ behavior: 'smooth' });
      mobileMenu.classList.remove('open');
    });
  });
  
  burgerBtn.addEventListener('click', () => mobileMenu.classList.add('open'));
  closeMenu.addEventListener('click', () => mobileMenu.classList.remove('open'));
  
  document.querySelectorAll('.mobile-menu .lang-mobile a').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      changeLocale(btn.getAttribute('data-lang'));
      mobileMenu.classList.remove('open');
    });
  });
  
  document.querySelectorAll('.lang-dropdown button').forEach(btn => {
    btn.addEventListener('click', () => changeLocale(btn.getAttribute('data-lang')));
  });
  
  langBtn.addEventListener('click', () => langDropdown.classList.toggle('active'));
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.lang-switcher')) langDropdown.classList.remove('active');
  });
  
  floatWhatsApp.addEventListener('click', () => {
    const msg = `${currentLocale === 'ka' ? 'გამარჯობა, მინდა შეფასება. გთხოვთ მომწეროთ დეტალები.' : currentLocale === 'en' ? 'Hello, I would like an assessment. Please send me details.' : 'Здравствуйте, хочу получить оценку. Пожалуйста, напишите детали.'}`;
    window.open(`https://wa.me/995593082255?text=${encodeURIComponent(msg)}`, '_blank');
    if (typeof gtag !== 'undefined') gtag('event', 'floating_whatsapp_click');
  });
  
  prevSlideBtn.addEventListener('click', prevSlide);
  nextSlideBtn.addEventListener('click', nextSlide);
  window.addEventListener('resize', () => updateGallery());
  
  // Initial load based on URL path
  const pathLocale = window.location.pathname.split('/')[1];
  if (['ka', 'en', 'ru'].includes(pathLocale)) currentLocale = pathLocale;
  else currentLocale = 'ka';
  applyLocale();
  renderGallery();
  buildStats();